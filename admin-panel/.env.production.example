# Production API URL. Set the real value as an environment variable in
# the Vercel dashboard (Project -> Settings -> Environment Variables) —
# Vercel's value overrides anything in a committed .env file at build time.
VITE_API_URL=https://zjai-bot.up.railway.app
